# Base URL of the running API (src/api/app.py). No trailing slash.
# Local dev default matches `uvicorn src.api.app:app --reload` on port 8000.
VITE_API_BASE=http://localhost:8000
# Live widget on /, /lawyers, /signup after `python examples/seed_flywheel_sales.py`.
VITE_SALES_BUSINESS_ID=flywheel
