# P0–P4 delivery — 3 September 2026

Local report for the sequential P0–P4 pass. Code lives in this repository; this file is the operator-facing summary.

Push is **not** done here. Alena pushes `main` after reviewing the local commits.

## P0 — close the deal, tell the truth

- Quote replies like “sounds good, lets do it” accept the **already presented** quote; conditions/deferrals stay unclear and are re-asked.
- “Option 2 please” books the second proposed slot.
- Unclear quote replies re-ask, then escalate only after the attempt cap.
- After `NEEDS_HUMAN`, later messages get distinct acknowledgments; low-confidence cases can re-enter qualification.
- Widget sends `customer_timezone`; junk values are dropped without a 4xx.
- Remote services show slot times in the visitor’s zone; booking confirmation can include both zones. Availability is still calculated in the business zone.
- Marketing no longer promises Google Calendar sync. End-customer payment is still not collected; copy says the business follows up.

## P1 — owner can configure without an engineer

- Settings: matching phrases (`intake_keywords`), AI disclosure, compliance line.
- Settings: CRM webhook URL (HTTPS, never read back).
- Dashboard/analytics period is filtered in SQL (`created_at`, test-data flag).

## P2 — multi-worker and durable side effects

- Migration `0020`: `rate_limit_hits`, `integration_outbox`.
- Public chat and account-security limits use the shared SQL limiter.
- CRM notify writes an outbox row, then delivers; cron: `POST /api/v1/internal/integrations/deliver`.
- Expiry sweep: `POST /api/v1/internal/commercial/expire` (same `INTERNAL_TASK_SECRET`).

## P3 — honest eval and cost logs

- `scripts/live_vertical_eval.py --runs N` reports `all_runs_correct_rate`, `any_run_correct_rate`, min/max per-run rates (not an average of lucky samples).
- Structured log event `ai_usage` (tokens, model, prompt id). No live paid eval was run in this pass.

## P4 — hygiene

- `docs/architecture.md` Alembic head is `0020`.
- Dead UI mockups moved to `archive/front/`.
- Frontend date helpers extracted; `npm test` in `web/app`; CI runs it.

## What is still deferred

- Charging the tenant’s customer (payment link / Stripe on their side).
- Syncing Flywheel bookings to Google Calendar.
- SMS outbox beyond the existing follow-up SMS outbox.
- UPL legal review before a paying CA/NY law-firm customer.
